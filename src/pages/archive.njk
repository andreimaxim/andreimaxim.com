---
layout: page.njk
title: Archive
description: Archive of posts by year.
permalink: /archive/
---

<section class="archive">
    <p class="archive-intro">Browse the full index of posts, organized by year.</p>
    {%- for year in collections.postsByYear -%}
    <section class="archive-year">
        <h2>{{ year.name }}</h2>
        <ol class="post-list post-list--compact">
            {%- for post in year.items -%}
            <li>
                <a href="{{ post.url }}">{{ post.data.title }}</a>
                <time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time>
            </li>
            {%- endfor -%}
        </ol>
    </section>
    {%- endfor -%}
</section>
