---
layout: default
---
{% include navbar.liquid %}

<div class="container prose max-w-6xl mx-auto px-4">
    <article class="prose prose-stone prose-xl max-w-none mx-auto mt-8 mb-16">
        <h1>{{ page.title }}</h1>
        <p class="text-gray-600">Written on {{ page.date | date: "%B %d, %Y" }}</p>
        {{ content }}
    </article>
</div>


